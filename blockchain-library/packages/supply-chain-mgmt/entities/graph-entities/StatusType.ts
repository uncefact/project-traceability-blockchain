/* eslint-disable no-unused-vars */
/* Enumerated type representing the possible statuses of a certificate. */
export enum StatusType {
    DEFAULT,
    FULLY_COMPLIANT,
    PARTIALLY_COMPLIANT,
    NOT_COMPLIANT,
}
